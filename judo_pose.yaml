path: datasets/judo_pose  # Root directory
train: train/images       # Train folder relative to path
val: val/images           # Val folder relative to path

# Keypoints (COCO format)
kpt_shape: [17, 3]
flip_idx: [0, 2, 1, 4, 3, 6, 5, 8, 7, 10, 9, 12, 11, 14, 13, 16, 15]

# Classes
names:
  0: person